services:
  registry:
    image: registry:2
    ports: 
    - container: 5000
      https: 443
    env_vars:
      REGISTRY_STORAGE: filesystem
      REGISTRY_STORAGE_FILESYSTEM_ROOTDIRECTORY: /var/lib/registry